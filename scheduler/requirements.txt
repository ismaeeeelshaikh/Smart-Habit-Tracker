apscheduler>=3.11,<4
sqlalchemy[asyncio]>=2.0,<3
psycopg2-binary>=2.9
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3

# The slim base image ships a trimmed IANA database that omits legacy
# aliases — Asia/Calcutta is missing while Asia/Kolkata is present, and
# browsers still send the former. Without this, a real user's timezone
# silently resolved to UTC and every time they saw was hours wrong.
tzdata>=2024.1
